<%= form_for([:admin,category]) do |f| %>
    <%= render 'common/errors', object: category %>

     <%= f.text_field :title %><%= f.submit t('.submit', action: (category.new_record? ? t('common.create') : t('common.update')))%>
<% end %>
